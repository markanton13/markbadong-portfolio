# Learning Library Portfolio Integration v2

This corrected patch integrates the Learning Library into `markbadong.com` without linking to routes that do not exist.

## Fixes

- Adds the Learning Library as a homepage featured project.
- Adds `/projects/learning-library` as a direct Vite route.
- Replaces nonexistent `/demo/` and `/architecture/` links with internal case-study anchors.
- Uses existing live pages only: the Learning Library homepage, pricing page, and support page.
- Expands the homepage capabilities from four to six cards.
- Adds Cloudflare Pages, Workers, D1, payment-webhook, access, recovery, analytics, and launch-control skills.
- Adds route-specific SEO and structured data for the Learning Library case study.

## Resume

The portfolio's current public resume PDF was not included in the uploaded files for this chat. `RESUME-UPDATE-BRIEF.md` records the exact verified additions to apply after the editable resume or current PDF is uploaded.
